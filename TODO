if not post don't check for CL or TE 

if (this->method == "POST")
{
    this->randomstr = "./cache/" + generateRandomString();
    this->body = open(this->randomstr.c_str(), O_WRONLY | O_CREAT | O_TRUNC, 0644);
    if (this->body == -1)
        throw std::invalid_argument("500 Unable to open file ");
}

last case generateErrpage 302
generateErrorPage(code);


else ind sendresp()


